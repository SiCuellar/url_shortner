defmodule UrlShortnerWeb.LinkCreatorTest do
  use ExUnit.Case
  alias UrlShortner.LinkCreator

  # test "it can take long url and return short url" do
  #   url = "https//hello-world.com"
  #   short_link = LinkCreator.generate_short_link(url)
  #   assert String.length(short_link) == 28
  # end
end
